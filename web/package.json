{
  "name": "meshwx-web",
  "private": true,
  "version": "0.1.0",
  "description": "MeshWX weather client for Chromium browsers: weather from the mesh, over Web Bluetooth or Web Serial",
  "type": "module",
  "scripts": {
    "dev": "node tools/dev-server.mjs",
    "test": "node --test test/",
    "package": "node tools/package.mjs --zip",
    "single": "node tools/single-file.mjs",
    "strings": "node tools/strings-to-json.mjs"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
